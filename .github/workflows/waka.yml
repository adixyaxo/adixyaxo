- uses: athul/waka-readme@master
        with:
          WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SHOW_TITLE: true
          BLOCKS: ░▒▓█
          CODE_LANG: python
          TIME_RANGE: last_7_days
          LANG_COUNT: 5
          SHOW_TIME: true
          # 👇 This is the fixed line:
          TARGET_PATH: "adixyaxo.md"
